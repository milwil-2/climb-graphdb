# Load the gitignored .env into the shell env so every tool in this repo —
# `claude` (and the ${NEO4J_*} refs its MCP servers need), `uvx`, `uv run` —
# sees the same vars the app already loads via python-dotenv in config.py.
# One-time per clone:  direnv allow
dotenv_if_exists .env
