# ── Secrets & credentials ─────────────────────────────────────────────────────
.env
.env.*
!.env.example
*.pem
*.key
*.crt
*credentials*
*secret*

# ── Data: large / fetched / proprietary ───────────────────────────────────────
data/**
!data/athlete_handles.yaml
!data/**/.gitkeep
*.db
*.sqlite
*.csv
*.parquet

# ── Build & tooling ───────────────────────────────────────────────────────────
.venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
coverage.xml
dist/
*.egg-info/
.vercel/

# ── Claude Code: personal overrides (shared settings.json IS committed) ───────
.claude/settings.local.json
# Agent worktrees (isolation: "worktree") are ephemeral scratch — never committed.
/.claude/worktrees/

# ── OS / editor ───────────────────────────────────────────────────────────────
.DS_Store
.idea/
.vscode/

# ── Local-only proprietary planning docs (never commit) ───────────────────────
# NOTE: CLAUDE.md IS committed (lean, rules-only agent guide; auto-loaded by
# Claude Code). Only the planning/design IP below stays local.
PRD.md
PLAN.md
IMPLEMENTATION_PLAN.md
